source "https://rubygems.org"

ruby '3.4.2' # Ou ta version actuelle de Ruby (ruby -v pour vérifier)

gem 'rspec'      # Pour les tests
gem 'nokogiri'   # Le coeur du réacteur pour parser le HTML
gem 'open-uri'   # Pour ouvrir les URLs (souvent inclus de base, mais utile à préciser)
gem 'pry'        # Indispensable pour debugger et tester tes variables en live